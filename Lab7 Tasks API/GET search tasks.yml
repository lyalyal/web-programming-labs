info:
  name: GET search tasks
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/tasks/search?status=pending
  params:
    - name: status
      value: pending
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
