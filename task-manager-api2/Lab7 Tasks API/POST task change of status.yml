info:
  name: POST task change of status
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:3000/tasks/1
  body:
    type: json
    data: |
      {
        "status": "done"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
