info:
  name: POST new task2
  type: http
  seq: 21

http:
  method: POST
  url: http://localhost:3000/tasks
  body:
    type: json
    data: |-
      {
        "title": "Whitelist test",
        "priority": "low",
        "isAdmin": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
